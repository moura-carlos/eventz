<header>
  <nav>
    <%= link_to image_tag("logo.png"), root_path %>
    <ul class="left">
      <li>
       <%= link_to "Upcoming Events", events_path %>
      </li>
      <li>
      <%# The parameter passed to #filtered_events_path can be a string or symbol %>
       <%= link_to "Past Events", filtered_events_path(:past)%>
      </li>
      <li>
       <%= link_to "Free Events", filtered_events_path(:free) %>
      </li>
      <li>
       <%= link_to "Recent Events", filtered_events_path(:recent) %>
      </li>
    </ul>
    <ul class="right">
      <% if current_user %>
        <li>
        <%# we need to figure out where to
        define 'current_user' method.
        we're calling it from a View,
        so it needs to be a View Helper Method.
        View Helper Methods live in a helper module
        we could put it inside of session_helper's module.
        Bu since we might use the 'current_user' helper method all over the application, so
        we'll put it in the global Application Helper (application helper.rb).
        %>
          <%= link_to current_user.name, current_user %>
        </li>
        <li>
          <%= link_to "Sign Out", session_path, data: { turbo_method: :delete },
          class: "button" %>
        </li>
      <% else %>
        <li>
          <%= link_to "Sign In", new_session_path, class: "button" %>
        </li>
        <li>
          <%= link_to "Sign Up", signup_path, class: "button" %>
        </li>
      <% end %>
    </ul>
  </nav>
</header>
